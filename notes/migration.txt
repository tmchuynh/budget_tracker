INITIALIZE FLASK-MIGRATE (ONLY NEEDED ONCE)
flask db init --> flask --app server db init

!!!!! Please edit configuration/connection/logging settings in 'C:\\Users\\tinah\\OneDrive\\Documents\\Projects\\budget_tracker\\migrations\\alembic.ini' before proceeding.

alembic.ini 
script_location = migrations
sqlalchemy.url = sqlite:///./test.db

env.py
-- Need to import all models at the top of file
AND
# Set up the SQLAlchemy URL for Alembic using environment variables
db_url = f"mysql+pymysql://{os.getenv('DB_USER')}:{os.getenv('DB_PASSWORD')}@{os.getenv('DB_HOST')}/{os.getenv('DB_NAME')}"
config.set_main_option('sqlalchemy.url', db_url)


GENERATE MIGRATION SCRIPTS
flask db migrate -m "Describe your migration" --> flask --app server db migrate -m "Describe your migration"

APPLY THE MIGRATION TO THE DATABASE
flask db upgrade --> flask --app server db upgrade

RENDER MIGRATION TO SQL AND SAVE TO MIGRATIONS DIRECTORY
flask --app server db upgrade --sql > migrations/migration_script.sql

flask run --> flask --app server run

flask --app server db history

flask --app server db stamp head

flask --app server db heads
flask --app server db merge