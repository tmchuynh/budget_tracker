# Ensure that all text files use LF line endings in the repository
* text=auto

# Mark image files as binary
*.png binary
*.jpg binary
*.gif binary


# Ensure .md files are treated as text
*.md text working-tree-encoding=UTF-8


# Normalize line endings to LF in the repository
*.sh text eol=lf
*.py text eol=lf


# Track large files with Git LFS
*.psd filter=lfs diff=lfs merge=lfs -text


# Treat XML files as text for diffing purposes
*.xml text diff=xml


# Ignore diffs for large generated files
*.log binary


